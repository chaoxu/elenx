# Supplementary reproducibility record

The audited case study is the campaign artifact `elenx-evaluations/imo-2011-p2/campaign.db` in the Elenx evaluation workspace. Its append-only entries contain the four candidates, all model calls and tool calls, and the four verifier verdicts. The artifact is retained separately from the anonymous submission and can be released after review.

The final comparative experiment should publish one settings file per arm, a task manifest with frozen hashes, raw campaign artifacts, a cost extraction script, and the adjudication record. Provider credentials and private endpoints must be removed before release.
